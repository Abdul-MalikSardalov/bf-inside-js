// #todo

/**
 *
 */
export const longToUpper = (arr = []) => {
  return arr.map((element) => element.toUpperCase());
};

// const arr = ['apple', 'banana', 'orange'];
// const upperCaseArr = arr.map((element) => element.toUpperCase());
// console.log(upperCaseArr); // ['APPLE', 'BANANA', 'ORANGE']
